# xrdb

```bash
# Merge a config file with the existing settings
xrdb -merge <config>

# Replace settings with config file
xrdb -load <config>

# List settings
xrdb -query

# Clear settings
xrdb -remove
```
